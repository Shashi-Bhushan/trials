public class ToDo{
	/*
	 * ToDO:
	 * 
	 * Difference
	 * Collection and Collections
	 * Object and Objects
	 *
	 * Ask on SO 
	 * 1. if list.size() can return only Integer.MAX_VALUE, then if a List has more then Integer.MAX_VALUE, efficient way to get length of such ArrayList.
	 */ 
}